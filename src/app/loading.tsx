export default function Loading() {
  return (
    <div className="fixed inset-0 z-[100] grid place-items-center bg-ink">
      <p className="section-label animate-pulse">Cargando experiencia</p>
    </div>
  );
}
